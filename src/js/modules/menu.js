'use strict';

function openMenu(selectorTrigger, menuSelector) {
    const trigger = document.querySelector(selectorTrigger),
          menu = document.querySelector(menuSelector);
    
    trigger.addEventListener('click', ()=> {

    });
}